{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-right mt-5">
        <a href="{{ path('app_backend_genre_index') }}" class="btn btn-outline-secondary">Annuler</a>
        <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
