{{ form_start(form) }}
    <div class="row form-group">
        <div class="col-6 mt-3">{{ form_row(form.famille) }}</div>
        <div class="col-6 mt-3">{{ form_row(form.titre) }}</div>
        <div class="col-12 mt-3">{{ form_row(form.description) }}</div>
        <div class="col-6 mt-3">{{ form_row(form.media) }}</div>
        {% if categorie.media %}
            <div class="col-6 mt-3">
                <img src="{{ asset('uploads/famille/' ~ categorie.media) }}" alt="{{ categorie.titre }}" class="img-fluid">
            </div>
        {% endif %}
        <div class="col-6 mt-3">{{ form_row(form.tags) }}</div>
        <div class="col-12 mt-3">
            <div class="custom-control custom-checkbox">
                {{ form_widget(form.statut) }}
                {{ form_label(form.statut) }}
            </div>
            {{ form_errors(form.statut) }}
        </div>
    </div>
    {{ form_widget(form) }}
    <div class="text-right mt-5">
        <a href="{{ path('app_backend_categorie_index') }}" class="btn btn-outline-secondary">Annuler</a>
        <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
