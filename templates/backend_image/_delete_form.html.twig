<form method="post" action="{{ path('app_backend_image_delete', {'id': image.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette image?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image.id) }}">
    <button class="btn btn-danger">supprimer</button>
</form>
