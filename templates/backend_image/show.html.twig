{% extends 'base.html.twig' %}

{% block title %}Image{% endblock %}

{% block body %}
    <h1>Image</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>Media</th>
                <td>{{ image.media }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ image.createdAt ? image.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_image_index') }}">back to list</a>

    <a href="{{ path('app_backend_image_edit', {'id': image.id}) }}">edit</a>

    {{ include('backend_image/_delete_form.html.twig') }}
{% endblock %}
